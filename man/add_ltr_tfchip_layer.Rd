% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_tfchip_layer.R
\name{add_ltr_tfchip_layer}
\alias{add_ltr_tfchip_layer}
\title{Add LTR TF ChIP-seq overlap evidence}
\usage{
add_ltr_tfchip_layer(
  x,
  tfchip_file = NULL,
  output_dir = NULL,
  verbose = TRUE,
  .return_full_features = FALSE
)
}
\arguments{
\item{x}{A \code{HERVarium_annotation} object or HERV-level \code{data.frame}.}

\item{tfchip_file}{Optional path to \code{ltr_tfchip_compact.tsv.gz}. If \code{NULL},
the bundled resource is used.}

\item{output_dir}{Optional output directory. For a \code{HERVarium_annotation}
object, \code{x$output_dir} is used when available.}

\item{verbose}{Logical. Whether to print progress messages.}

\item{.return_full_features}{Internal use only. Whether to retain and return
the full intermediate feature matrix instead of applying compact
user-facing pruning.}
}
\value{
The updated object or feature \code{data.frame}. By default, its
user-facing feature table is compact; \code{.return_full_features = TRUE} is
reserved for internal workflow composition.
}
\description{
Adds compact TF ChIP-seq peak-cluster overlap annotations for the associated
5′ and 3′ LTRs of each HERV. The layer is joined through the LTR names stored
in the HERVariumR feature table.
}
\details{
TF ChIP-seq overlap is experimental positional context. It is distinct from
sequence-based TFBM prediction and does not by itself prove autonomous TF
recruitment by a HERV sequence.
}
