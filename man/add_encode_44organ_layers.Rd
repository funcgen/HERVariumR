% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_encode_44organ_layers.R
\name{add_encode_44organ_layers}
\alias{add_encode_44organ_layers}
\title{Add HERVarium ENCODE/UCSC 44-organ external-evidence layers}
\usage{
add_encode_44organ_layers(
  x,
  internal_encode_file = NULL,
  ltr_encode_file = NULL,
  output_dir = NULL,
  verbose = TRUE,
  .return_full_features = FALSE
)
}
\arguments{
\item{x}{A \code{HERVarium_annotation} object or HERV-level \code{data.frame}.}

\item{internal_encode_file}{Optional path to
\code{internal_encode_44organs_compact.tsv.gz}. If \code{NULL}, the bundled resource
is used.}

\item{ltr_encode_file}{Optional path to
\code{ltr_encode_44organs_compact.tsv.gz}. If \code{NULL}, the bundled resource is
used.}

\item{output_dir}{Optional output directory. For a \code{HERVarium_annotation}
object, \code{x$output_dir} is used when available.}

\item{verbose}{Logical. Whether to print progress messages.}

\item{.return_full_features}{Internal use only. Whether to retain and return
the full intermediate feature matrix instead of applying compact
user-facing pruning.}
}
\value{
The updated object or feature \code{data.frame}. By default, its
user-facing feature table is compact; \code{.return_full_features = TRUE} is
reserved for internal workflow composition.
}
\description{
Adds compact internal-region and LTR-level ENCODE/UCSC 44-organ evidence to
a \code{HERVarium_annotation} object or HERV-level feature table.
}
\details{
Internal evidence is joined by \code{HERV_id} through the compact precomputed
bridge. Associated LTR evidence is joined separately through \code{ltr5_name} and
\code{ltr3_name}. In the signal hierarchy, high evidence is a subset of robust
evidence, which is a subset of detected evidence.
}
